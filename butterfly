# Butterfly Pattern  
  
rows = int(input("Enter number of rows: "))  
  
# Upper half  
for i in range(1, rows + 1):  
    # Left wing  
    print("*" * i + " " * (2 * (rows - i)) + "*" * i)  
  
# Lower half  
for i in range(rows, 0, -1):  
    # Right wing  
    print("*" * i + " " * (2 * (rows - i)) + "*" * i)  
